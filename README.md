# Ariabl API
The API layer for Ariabl, a full stack application for developers to efficiently make their frontend code more accessible

# Contributors
Mason DeWitt (mason.dewitt@snhu.edu)

# Running the App

## In Development
 - First run `npm install` to install all necessary depencies
 - Next, run `npm run dev` to start the server
 - The server will be locally hosted at the set port found in config/default.ts